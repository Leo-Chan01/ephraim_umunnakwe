import 'package:flutter/material.dart';

class BrandPromiseModel {
  int id;
  String title;
  String subTitle;
  IconData prandPromiseIcon;

  BrandPromiseModel(
      {required this.id,
      required this.title,
      required this.subTitle,
      required this.prandPromiseIcon});
}
